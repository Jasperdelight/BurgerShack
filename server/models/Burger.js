export class Burger {
  constructor(data) {
    this.id = // needs an id
      this.name = data.name
    this.hasBacon = data.hasBacon
    // expand this model with things like ingredients and price
    // hasBacon?
  }
}